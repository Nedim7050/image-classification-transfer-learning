# Core dependencies (required)
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
pillow>=10.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
streamlit>=1.28.0
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.65.0
tensorboard>=2.13.0
jupyter>=1.0.0
ipywidgets>=8.0.0

# Optional dependencies for ONNX conversion
# Note: onnx and onnxruntime may not be available for Python 3.14+
# These are optional - the project works without them
# Install manually if needed: pip install onnx onnxruntime

